// 2- Dada a array const doces = ['sorvete', 'pirulito', 'bombom'];
// Adicione um novo item de sua escolha no começo dessa array.

const doces = ['sorvete', 'pirulito', 'bombom'];
//const adicionar = doces.unshift('milkshake') < altera a const a partir daqui como se esse item fizesse pt 4ever
doces.unshift('milkshake') //so altera a const nesse momento

console.log(doces)